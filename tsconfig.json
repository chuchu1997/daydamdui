{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": "src",  // Thiết lập base URL cho thư mục "src"
    "paths": {
      "@/*": ["*"]  // Alias "@" trỏ tới thư mục "src"
    }
  }
}
